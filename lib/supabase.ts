/**
 * Supabase Client Configuration
 *
 * PHASE 2 IMPLEMENTATION — This is a placeholder for now.
 * The app currently uses mock data (see data/mockSales.ts).
 *
 * To connect to Supabase:
 * 1. Create a free project at https://supabase.com
 * 2. Run the SQL migration in supabase/migrations/001_initial_schema.sql
 * 3. Replace the placeholder values below with your project's URL and anon key
 * 4. Install: npm install @supabase/supabase-js
 */

// import { createClient } from '@supabase/supabase-js';
//
// const SUPABASE_URL = 'https://your-project.supabase.co';
// const SUPABASE_ANON_KEY = 'your-anon-key';
//
// export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY);

export const SUPABASE_CONFIGURED = false;
